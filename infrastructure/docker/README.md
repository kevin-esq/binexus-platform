# Application Dockerfiles

Placeholder directory for per-app Dockerfiles. Will be filled out when we deploy beyond local development. For now we only run dependencies via `infrastructure/compose/docker-compose.yml`.

Planned files (intentionally absent in Phase 0):

- `backend.Dockerfile`
- `web.Dockerfile`
