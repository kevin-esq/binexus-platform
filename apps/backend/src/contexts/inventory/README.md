# Inventory bounded context

Status: **placeholder** (Phase 2).

Stock per branch, movements, reservations, transfers. Subscribes to `ORDER_APPROVED` to reserve stock and to `ORDER_CANCELLED` to release reservations.
